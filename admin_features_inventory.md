# Admin Features Inventory

This document provides a comprehensive, granular inventory of every feature, tool, and operational workflow currently available or planned in the Admin Department of the Creative Marketplace.

---

| Feature | Purpose | Frontend Route | API Endpoint(s) | Database Tables | Status | Progress % | Problem | Next Action |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **Admin Dashboard Overview** | Real-time executive metrics, KPI cards, and Needs Attention queue | `/admin/dashboard` | `GET /api/v1/admin/dashboard` | `bookings`, `projects`, `users`, `payments`, `payouts`, `disputes`, `freelancer_verifications`, `booking_assignments` | **COMPLETE** | 100% | None | Maintain & monitor real-time aggregates. |
| **Admin Analytics** | Filtered marketplace trends, volume, and operational metrics | `/admin/dashboard` | `GET /api/v1/admin/analytics` | `bookings`, `projects`, `payments` | **COMPLETE** | 100% | None | Maintain analytics caching if traffic grows. |
| **Booking Inbox** | Triage and manage incoming client direct booking requests | `/admin/bookings` | `GET /api/v1/admin/bookings` | `bookings`, `users`, `freelancer_profiles`, `booking_assignments` | **COMPLETE** | 100% | None | Real backend filtering and status triage functional. |
| **Booking Control Center** | Detailed inspection, assignment management, and client communication | `/admin/bookings/[id]` | `GET /api/v1/admin/bookings/{id}`, `POST /api/v1/admin/bookings/{id}/review` | `bookings`, `booking_assignments`, `conversations`, `messages`, `users` | **COMPLETE** | 100% | None | Full operational workflow functional. |
| **Freelancer Assignment Engine** | Assign primary creators or replacements with offered payout | `/admin/bookings/[id]` | `POST /api/v1/admin/bookings/{id}/assign` | `booking_assignments`, `bookings`, `freelancer_profiles`, `admin_audit_logs` | **COMPLETE** | 100% | None | Supports multi-round assignments and replacement flags. |
| **Job Posts Management** | Review open client project briefs and track matching status | `/admin/job-posts`, `/admin/job-posts/[id]` | `GET /api/v1/admin/projects`, `GET /api/v1/admin/projects/{id}`, `POST /api/v1/admin/projects/{id}/review` | `projects`, `bookings`, `users`, `conversations` | **COMPLETE** | 100% | Dashboard link points to `/admin/bookings` instead of `/admin/job-posts` | Update link in `src/app/admin/dashboard/page.tsx:394`. |
| **Direct Project Matching** | Match and assign creators to projects directly without proposals | `/admin/job-posts/[id]` | `POST /api/v1/admin/projects/{id}/match` | `projects`, `bookings`, `booking_assignments`, `freelancer_profiles` | **COMPLETE** | 100% | None | Creates managed booking and assignment seamlessly without proposals. |
| **Assignments Pipeline** | Global assignment tracking across offered, accepted, and counter-offered states | `/admin/assignments` | `GET /api/v1/admin/bookings` (Assignment filter) | `booking_assignments`, `bookings`, `users`, `freelancer_profiles` | **COMPLETE** | 100% | None | Filter tabs for Counter Offers, Awaiting Freelancer, and Approval working. |
| **Active Jobs Monitoring** | Monitor jobs in execution (deposit paid, ready to start, in progress) | `/admin/active-jobs` | `GET /api/v1/admin/bookings` (Active filter) | `bookings`, `payments`, `booking_assignments` | **COMPLETE** | 100% | None | State mapping across deposit and progress lifecycle works. |
| **Deliveries & Submissions Queue** | Quality gate inbox for reviewing freelancer work submissions | `/admin/deliveries` | `GET /api/v1/admin/deliveries` | `deliveries`, `delivery_files`, `bookings`, `revision_requests` | **BROKEN** | 40% | `ModuleNotFoundError: No module named 'app.models.freelancer'` in `admin_management.py:1393` causes 500 error | Fix import to `app.models.freelancer_profile` and add action modals. |
| **Submission Review Actions** | Approve work, request revisions, mark balance required, and deliver final work | `/admin/deliveries` | `POST /api/v1/deliveries/{id}/approve`, `POST /api/v1/deliveries/{id}/share-draft`, `POST /api/v1/deliveries/{id}/deliver-final`, `POST /api/v1/revisions/requests` | `deliveries`, `revision_requests`, `bookings` | **PARTIAL** | 35% | Deliveries page crashes on load; UI lacks integrated action buttons | Fix deliveries API and integrate review action modals in frontend. |
| **Completed Jobs History** | Audit completed projects, ratings, balance clearances, and payout releases | `/admin/completed-jobs` | `GET /api/v1/admin/completed-jobs` | `bookings`, `reviews`, `payouts`, `users` | **BROKEN** | 40% | `ModuleNotFoundError` (`admin_management.py:1443`) and `review.rating` attribute mismatch cause 500 error | Fix import to `app.models.freelancer_profile` and update attribute to `review.overall_rating`. |
| **Admin Messages Console** | Mediated communication hub for CLIENT_ADMIN and FREELANCER_ADMIN threads | `/admin/messages` | `GET /api/v1/admin/conversations`, `GET /api/v1/messages/conversations/{id}`, `POST /api/v1/messages/conversations/{id}/messages` | `conversations`, `messages`, `conversation_participants`, `message_attachments` | **PARTIAL** | 80% | Sidebar navigation is disabled (`disabled: true` in `Sidebar.tsx:52`) | Enable "Messages" link in `Sidebar.tsx`. |
| **User & Account Moderation** | Suspend or reactivate user accounts with reason tracking | `/admin/users` | `GET /api/v1/admin/users`, `POST /api/v1/admin/users/{id}/suspend`, `POST /api/v1/admin/users/{id}/reactivate` | `users`, `admin_audit_logs` | **COMPLETE** | 100% | None | Search, filter by role (Client/Freelancer/Admin), and suspension working. |
| **Client Management Hub** | Dedicated client profile, booking history, and project timeline view | Disabled in sidebar | `GET /api/v1/admin/users?role=CLIENT` | `users`, `bookings`, `projects` | **PARTIAL** | 50% | No standalone `/admin/clients` frontend page; disabled in sidebar | Build dedicated `/admin/clients` page with timeline. |
| **Freelancer Management Hub** | Dedicated freelancer directory with performance, badges, and earnings | Disabled in sidebar | `GET /api/v1/admin/freelancers`, `GET /api/v1/admin/freelancers/{id}`, `POST /api/v1/admin/freelancers/{id}/badge` | `freelancer_profiles`, `users`, `trust_badges`, `freelancer_badges`, `ledger_entries` | **PARTIAL** | 50% | No standalone `/admin/freelancers` frontend page; disabled in sidebar | Build dedicated `/admin/freelancers` page and enable sidebar link. |
| **Service Moderation** | Hide or restore public marketplace service listings | Disabled in sidebar | `GET /api/v1/admin/services`, `POST /api/v1/admin/services/{id}/hide`, `POST /api/v1/admin/services/{id}/restore` | `services`, `freelancer_profiles` | **PARTIAL** | 40% | Backend endpoints exist (200 OK), but no frontend `/admin/services` page exists | Build `/admin/services` page and enable sidebar link. |
| **Service Categories Architecture** | Category CRUD and active status management | Disabled in sidebar | `GET /api/v1/admin/categories`, `POST /api/v1/admin/categories`, `PATCH /api/v1/admin/categories/{id}` | `service_categories` | **BROKEN** | 20% | DB still has old `Editing` category and is missing `Editor`, `3D Animator`, `Graphics`; no UI page | Migrate database categories and build `/admin/categories` page. |
| **Freelancer Verification Queue** | Review submitted ID documents, approve/reject verifications, award badges | `/admin/verification` | `GET /api/v1/admin/verifications`, `GET /api/v1/admin/verifications/{id}`, `POST /api/v1/admin/verifications/{id}/approve`, `POST /api/v1/admin/verifications/{id}/reject`, `GET /api/v1/admin/verifications/{id}/documents/{doc_id}/download` | `freelancer_verifications`, `verification_documents`, `freelancer_profiles`, `admin_audit_logs` | **COMPLETE** | 100% | None | Full secure document streaming and review workflow functional. |
| **Admin Payments & Gateway** | View client deposit/balance transactions and provider IDs | Disabled in sidebar | `GET /api/v1/admin/payments`, `GET /api/v1/admin/payments/{id}` | `payments`, `payment_attempts`, `payment_webhook_events` | **PARTIAL** | 50% | Backend APIs work (200 OK), but no frontend `/admin/payments` page exists | Build `/admin/payments` page and enable sidebar link. |
| **Refunds Management** | Process client refund claims with financial ledger reversals | Disabled in sidebar | `GET /api/v1/admin/refunds`, `POST /api/v1/admin/refunds/{id}/approve`, `POST /api/v1/admin/refunds/{id}/reject` | `refunds`, `payments`, `bookings`, `admin_audit_logs`, `ledger_entries` | **PARTIAL** | 50% | Backend APIs work (200 OK), but no frontend `/admin/refunds` page exists | Build `/admin/refunds` page and enable sidebar link. |
| **Payouts Management** | Disburse funds to creators, process bank transfers, and retry failures | Disabled in sidebar | `GET /api/v1/admin/payouts`, `POST /api/v1/admin/payouts/{id}/process`, `POST /api/v1/admin/payouts/{id}/retry` | `payouts`, `freelancer_profiles`, `ledger_entries`, `admin_audit_logs` | **PARTIAL** | 50% | Backend APIs work (200 OK), but no frontend `/admin/payouts` page exists | Build `/admin/payouts` page and enable sidebar link. |
| **Advance / Locked Earnings** | Track advance fee credits locked in escrow until booking completion | Backend ledger | `GET /api/v1/freelancer-earnings/overview` | `ledger_entries` | **COMPLETE** | 100% | None | Correctly maintains LOCKED vs AVAILABLE separation. |
| **Reviews Moderation** | Moderate public customer ratings and reviews | Disabled in sidebar | `GET /api/v1/admin/reviews`, `POST /api/v1/admin/reviews/{id}/hide`, `POST /api/v1/admin/reviews/{id}/restore` | `reviews`, `review_reports` | **BROKEN** | 20% | `AttributeError: 'Review' object has no attribute 'freelancer_profile'` in `admin_management.py:913` crashes API | Fix relationship attribute to `r.freelancer` and build `/admin/reviews` UI page. |
| **Disputes Resolution Pipeline** | Triage claims, communicate with parties, download evidence, and resolve | `/admin/disputes` | `GET /api/v1/admin/disputes`, `GET /api/v1/admin/disputes/{id}`, `POST /api/v1/admin/disputes/{id}/assign`, `POST /api/v1/admin/disputes/{id}/message`, `POST /api/v1/admin/disputes/{id}/resolve`, `GET /api/v1/admin/disputes/{id}/evidence/{ev_id}/download` | `disputes`, `dispute_messages`, `dispute_evidence`, `admin_audit_logs`, `refunds` | **COMPLETE** | 100% | None | Full dispute resolution and evidence handling functional. |
| **Platform Settings** | Global platform fees, escrow holds, and system flags configuration | `/admin/settings` | `GET /api/v1/admin/settings`, `PATCH /api/v1/admin/settings/{key}` | `platform_settings`, `admin_audit_logs` | **COMPLETE** | 100% | None | Full inline edit, DB persistence, and audit logging functional. |
| **Admin Audit Logs** | Comprehensive security ledger tracking administrative actions | `/admin/audit` | `GET /api/v1/admin/audit-logs` | `admin_audit_logs`, `users` | **COMPLETE** | 100% | None | Action filtering, metadata JSON inspection, and timestamps functional. |
| **Role Authorization (RBAC)** | Strict access enforcement blocking non-admin users from admin routes/APIs | All Admin routes | `require_role("ADMIN")` dependency on FastAPI | `users` | **COMPLETE** | 100% | None | Unauthenticated (401), Client (403), Freelancer (403) all enforced. |
